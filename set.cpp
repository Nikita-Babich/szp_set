#include <stdio.h>
#include <math.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>

//new pointers
int* a = (int*) malloc(100 * sizeof(int));
int* b = (int*) malloc(100 * sizeof(int));
int* res = (int*) malloc(100 * sizeof(int));

void intersect(){
}

void unite(){
}

main(){
	
}